namespace KrishiLink.Models.Entities
{
    /// <summary>The land unit a user thinks in. Decimal (shotangsho) is the platform's storage unit and the default.</summary>
    public enum LandUnit
    {
        Decimal,
        Katha,
        Bigha,
        Acre
    }
}
